import { useId } from "react";

import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";

export default function UserContactInput({contacts, setContacts}: {contacts: string[], setContacts: (contacts: string[]) => void}) {
  const id = useId();
  return (

  );
}
